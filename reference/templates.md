# 模板集合

本文档提供常见编写场景的模板，适用于TRS系统功能设计文档。

## 目录

1. [查询功能模板](#模板1-查询功能)
2. [新增功能模板](#模板2-新增功能)
3. [编辑功能模板](#模板3-编辑功能)
4. [删除功能模板](#模板4-删除功能)
5. [导出功能模板](#模板5-导出功能)
6. [上传功能模板](#模板6-上传功能)
7. [复制功能模板](#模板7-复制功能)
8. [级联选择功能模板](#模板8-级联选择功能)
9. [追溯谱系与召回模板](#模板9-追溯谱系与召回)
10. [数据项规范模板](#模板10-数据项规范)
11. [PRD质控报告模板](#模板11-prd质控报告)

---

## 模板1: 查询功能

### 场景询问清单

```markdown
## 查询功能询问框架

### 查询条件
- 有哪些查询条件？
- 每个条件的查询方式？
  - [ ] 模糊查询（如：名称包含XX）
  - [ ] 精确查询（如：编码等于XX）
  - [ ] 范围查询（如：日期在XX到XX之间）
  - [ ] 下拉选择（如：状态等于XX）
- 查询条件为空时，显示什么？
  - [ ] 显示全部数据
  - [ ] 提示"请输入查询条件"
  - [ ] 显示空列表

### 查询结果
- 查询结果包含哪些字段？
- 默认排序规则？（按XX升序/降序）
- 分页规则？（每页多少条？默认第几页？）
- 是否支持导出？

### 交互
- 查询按钮在哪里？
- 重置按钮在哪里？
- 是否需要"展开/收起"更多查询条件？
```

### 模板示例

```markdown
### 1. 查询功能

**查询条件：**
| 字段名 | 控件类型 | 查询方式 | 说明 |
|--------|---------|---------|------|
| 文件类型名称 | 文本框 | 模糊查询 | 支持模糊匹配 |
| 物料类别 | 下拉框 | 精确查询 | 必选 |
| 状态 | 下拉框 | 精确查询 | 全部/启用/禁用 |

**查询规则：**
- 查询条件非必填，查询条件置空进行查询时展示系统内全量数据
- 查询结果按文件类型编码升序展示
- 默认每页显示20条，支持分页

**查询结果：**
| 字段名 | 说明 |
|--------|------|
| 文件类型编码 | 唯一标识 |
| 文件类型名称 | 显示名称 |
| 物料类别 | 关联显示 |
| 状态 | 启用/禁用 |
| 操作 | 编辑/删除 |
```

---

## 模板2: 新增功能

### 场景询问清单

```markdown
## 新增功能询问框架

### 必填字段
- 需要填写哪些字段？
- 哪些字段是必填的？
- 是否有自动生成的字段？（编码、日期）
- 是否需要验重？哪些字段需要验重？

### 值列表
- 哪些字段有值列表？
  - 取哪个表？
  - 取哪个快码？
  - 级联关系？
- 是否支持搜索？
- 单值是否默认带出？

### 保存逻辑
- 保存前需要校验什么？
- 保存后跳转到哪里？
  - [ ] 返回列表（刷新列表）
  - [ ] 留在当前页面（继续新增）
  - [ ] 跳转到编辑页面
- 保存成功/失败的提示？
```

### 模板示例

```markdown
### 2. 新增功能

**必填字段：**
| 字段名 | 控件类型 | 必填 | 说明 |
|--------|---------|------|------|
| 文件类型编码 | 文本框 | 是 | 自动生成，格式：物料类别代码+3位流水号 |
| 文件类型名称 | 文本框 | 是 | 最多50字符 |
| 物料类别 | 下拉框 | 是 | 级联实体类型 |
| 文件类型 | 下拉框 | 是 | 取快码XX |
| 分类要素 | 下拉框 | 是 | 取快码XX |
| 状态 | 开关 | 是 | 默认启用 |

**值列表说明：**
- **物料类别**：取快码Item_Categories，支持搜索
- **文件类型**：级联物料类别，取所选物料类别下的文件类型
- **分类要素**：级联物料类别，取所选物料类别下的分类要素

**保存逻辑：**
1. 保存时校验文件类型名称不允许重复
2. 校验失败，报错："当前文件类型名称已存在，请重新命名"
3. 校验通过，保存数据，自动生成编码
4. 保存成功，弹出提示，返回列表并刷新
```

---

## 模板3: 编辑功能

### 场景询问清单

```markdown
## 编辑功能询问框架

### 可编辑字段
- 哪些字段可以编辑？
- 哪些字段不可编辑？（编码类通常不可编辑）
- 编辑后是否需要审核？

### 状态变更
- 状态类字段编辑后是否影响其他数据？
- 失效时是否级联失效子数据？

### 保存逻辑
- 保存前需要校验什么？
- 保存后跳转/提示？
```

### 模板示例

```markdown
### 3. 编辑功能

**可编辑字段：**
| 字段名 | 可编辑 | 不可编辑原因 |
|--------|--------|-------------|
| 文件类型名称 | ✅ | - |
| 物料类别 | ❌ | 已使用，修改会影响关联数据 |
| 文件类型 | ✅ | - |
| 分类要素 | ✅ | - |
| 状态 | ✅ | - |
| 失效日期 | ✅ | - |

**编辑限制：**
- 文件类型编码不可编辑
- 物料类别不可编辑（已使用）
- 修改物料类别会同步修改关联的链路信息

**状态变更逻辑：**
- 失效时，同步将文件类型下的文件类型分类及文件类型分类下的关键字段失效
- 启用时，不级联启用下级数据
```

---

## 模板4: 删除功能

### 场景询问清单

```markdown
## 删除功能询问框架

### 删除类型
- [ ] 物理删除（数据永久删除）
- [ ] 逻辑删除（标记为无效）

### 依赖检查
- 删除前需要检查什么？
- 在哪些表中被引用？
- 如果被引用，如何处理？
  - [ ] 报错，不允许删除
  - [ ] 提示，但允许删除

### 确认提示
- 是否需要确认弹窗？
- 确认消息内容？
```

### 模板示例

```markdown
### 4. 删除功能

**删除类型：** 逻辑删除（标记为禁用）

**删除逻辑：**
1. 删除前进行检查，检查当前文件类型在链路定义、地图配置中是否被引用
2. 若已被引用，报错不允许删除
   - 报错消息："当前数据已被使用，不允许删除，请失效当前数据或调整其他配置后删除"
3. 若其他关联功能未使用，弹出确认窗口
   - 弹窗消息："删除后无法恢复，是否确认删除"
4. 用户确认后，执行删除操作
5. 删除成功，弹出提示，刷新列表
```

---

## 模板5: 导出功能

### 场景询问清单

```markdown
## 导出功能询问框架

### 导出范围
- [ ] 当前查询结果
- [ ] 全部数据
- [ ] 选中数据

### 导出格式
- [ ] Excel (.xlsx)
- [ ] CSV (.csv)
- [ ] PDF

### 导出字段
- [ ] 所有字段
- [ ] 用户选择字段
- [ ] 固定字段

### 其他
- 是否需要导出模板？
- 文件命名规则？
- 大数据量导出是否需要异步？
```

### 模板示例

```markdown
### 5. 导出功能

**导出范围：** 当前查询结果

**导出格式：** Excel (.xlsx)

**导出字段：** 用户选择字段（默认全选）

**导出规则：**
1. 点击导出按钮，弹出字段选择弹窗
2. 用户选择要导出的字段（默认全选）
3. 确认后，生成Excel文件并下载
4. 文件命名为：文件类型配置_YYYYMMDD_HHmmss.xlsx

**大数据量处理：**
- 导出数据量>10000条时，提示用户"数据量较大，请耐心等待"
- 导出过程显示进度条
```

---

## 模板6: 上传功能

### 场景询问清单

```markdown
## 上传功能询问框架

### 文件限制
- 支持哪些格式？（PDF/Word/Excel/图片等）
- 单文件大小限制？
- 批量上传数量限制？

### 上传方式
- [ ] 普通上传（选择文件）
- [ ] 拖拽上传
- [ ] 两者都支持

### 上传交互
- 是否显示上传进度？
- 上传失败如何处理？
- 是否需要预览？

### 存储规则
- 上传后存储在哪里？
- 文件命名规则？
```

### 模板示例

```markdown
### 6. 上传功能

**文件限制：**
| 类型 | 限制 |
|------|------|
| 格式 | PDF、Word、Excel、图片(JPG/PNG) |
| 单文件大小 | 不超过20MB |
| 批量数量 | 一次最多10个文件 |

**上传方式：** 普通上传 + 拖拽上传

**上传交互：**
1. 点击上传按钮或拖拽文件到上传区域
2. 显示上传进度条
3. 上传成功，显示文件列表（文件名、大小、上传时间）
4. 上传失败，显示错误原因
   - 格式错误："文件格式不支持，仅支持PDF、Word、Excel、图片格式"
   - 大小超限："文件大小超过20MB，请压缩后上传"

**存储规则：**
- 上传文件存储到文件服务器
- 文件命名规则：UUID_原始文件名
```

---

## 模板7: 复制功能

### 场景询问清单

```markdown
## 复制功能询问框架

### 复制内容
- 复制哪些数据？
- 哪些数据不复制？（如：编码）

### 复制后
- 是否跳转到编辑页面？
- 默认值是否带出？
- 是否需要修改名称？（如：原名称+副本）
```

### 模板示例

```markdown
### 7. 复制功能

**复制内容：**
- 复制所有字段值（除编码外）
- 编码自动生成新值
- 名称默认：[原名称]_副本

**复制后流程：**
1. 点击复制按钮
2. 自动生成新编码和名称
3. 跳转到新增页面，填充已复制的数据
4. 用户可修改后保存
```

---

## 模板8: 级联选择功能

### 场景询问清单

```markdown
## 级联选择功能询问框架

### 级联关系
- 有哪些级联字段？
- 选择A后，B的取值规则？
- 选择B后，C的取值规则？

### 空值处理
- 切换上级值时，下级是否清空？
- 是否需要提示？

### 反向联动
- 选择C后，B和A是否需要联动？
```

### 模板示例

```markdown
### 8. 级联选择功能

**级联关系：**
```
物料类别 → 文件类型 → 分类要素
```

**级联规则：**
| 上级选择 | 下级变化 |
|---------|---------|
| 选择物料类别 | 文件类型取所选物料类别下的值，分类要素清空 |
| 选择文件类型 | 分类要素取所选文件类型下的值 |
| 切换物料类别 | 文件类型清空，分类要素清空 |
| 切换文件类型 | 分类要素清空 |

**交互说明：**
- 选择上级后，显示加载状态
- 下级数据更新后，自动刷新下拉列表
- 切换上级值时，提示"切换将清空下级选择，是否继续？"
```

---

## 通用模板片段

### 校验规则模板

```markdown
**校验规则：**

| 校验类型 | 字段 | 校验时机 | 校验条件 | 报错信息 |
|---------|------|---------|---------|---------|
| 格式校验 | 日期字段 | 保存时 | 日期格式YYYY-MM-DD | "[字段名]格式错误，请使用YYYY-MM-DD格式" |
| 长度校验 | 文本字段 | 保存时 | 不超过50字符 | "[字段名]长度不能超过50个字符" |
| 验重校验 | 名称字段 | 保存时 | 不允许重复 | "[字段名]已存在，请重新命名" |
| 组合校验 | 日期区间 | 保存时 | 开始日期<结束日期 | "[开始日期]必须早于[结束日期]" |
```

### 按钮交互模板

```markdown
**按钮说明：**

| 按钮 | 位置 | 作用 | 状态 |
|------|------|------|------|
| 查询 | 查询区 | 点击后根据当前已输入查询条件进行查询 | 始终可用 |
| 重置 | 查询区 | 点击清空当前已输入查询条件 | 始终可用 |
| 新增 | 列表区 | 点击新增，跳转到新增界面 | 始终可用 |
| 编辑 | 列表区 | 点击编辑，进入当前记录的编辑界面 | 始终可用 |
| 删除 | 列表区 | 点击删除，弹出确认窗口 | 数据未使用时可用 |
| 导出 | 列表区 | 点击导出，弹出字段选择窗口 | 有查询结果时可用 |
```

### 字段说明模板

```markdown
**字段：[字段名称]**

| 属性 | 说明 |
|------|------|
| 控件类型 | [文本框/下拉框/日期/数值/开关等] |
| 数值类型 | [字符/日期/数值/布尔] |
| 长度 | [最大字符数/精度] |
| 必填 | [是/否] |
| 新增可编辑 | [是/否] |
| 编辑可编辑 | [是/否] |
| 默认值 | [无/固定值/动态计算] |
| 值列表来源 | [取XX表/快码XX/手工输入] |
| 校验规则 | [格式/长度/验重/逻辑] |

**值列表说明：**
- 取数来源：[具体来源]
- 级联关系：[选择A后B取什么]
- 默认规则：[单值是否默认带出]
- 展示要求：[显示名称还是值]
- 支持搜索：[是/否]
```

---

**相关文件：**
- 主文件 → [SKILL.md](../SKILL.md)
- 章节结构 → [document-structure.md](document-structure.md)
- 示例库 → [examples.md](examples.md)

---

## 模板9: 追溯谱系与召回

### 场景询问清单

```markdown
## 追溯谱系与召回询问框架

### 追溯粒度与对象
- 追溯对象是什么？（原材料/在制/成品/序列号）
- 粒度是什么？（批次/箱/件/序列号）
- 需要支持正向追溯、反向追溯，还是两者？

### 谱系关系口径
- 是否存在拆分/合并/返工/报废/转产？
- 多对多关系如何记录数量分摊？（质量平衡/单位换算）
- 允许修改谱系关系吗？如何更正与留痕？

### 召回/演练
- 触发输入是什么？（原材料批次/成品批次/序列号/供应商批次）
- 影响范围要覆盖哪些状态？（在制/库存/在途/已出货）
- 目标时限是多少？（例如 X 分钟内出具全量清单）
- 需要导出哪些证据？（谱系图、出货清单、库存清单、检验记录、操作留痕）

### 审计与权限
- 哪些操作必须留痕？（修改主数据/更正/补打标签/冻结解冻）
- 权限如何分离？（录入/审批/放行/更正）

### 集成与采集
- 数据来自哪些系统？（ERP/MES/WMS/QMS）
- 现场采集点位有哪些？（入库/领料/工序/包装/出库）
- 离线/弱网如何处理？（缓存补传、冲突处理）
```

### 模板示例

```markdown
### 9. 追溯谱系与召回

**追溯粒度：** 成品批次 + 箱码
**追溯范围：** 正向 + 反向

**谱系关系：**
- 支持拆分/合并：是
- 数量口径：按净重（kg）分摊，单位换算来源为物料主数据
- 更正机制：仅允许追加更正单据，禁止覆盖；全量留痕（前后值+原因+审批）

**反向追溯输出（成品 → 输入）：**
- 输入：FGLOT-20260330-01
- 输出：原材料批次清单、工序记录、检验记录、包装记录
- 时限：5 分钟内出具
- 证据包：Excel + PDF（含查询条件、口径说明、操作留痕）

**正向追溯输出（输入 → 影响成品）：**
- 输入：RMLOT-20260301-08
- 输出：影响的在制/库存/已出货清单
- 处置动作：支持冻结库存、生成召回通知清单

**召回演练：**
- 退出标准：清单覆盖率 100%，口径一致，证据包可复核
```

> 领域细节模板见 `reference/traceability-domain.md`（建议在文档中直接引用/复用其中表格）。

---

## 模板10: 数据项规范

本模板规定数据项表格的标准格式，适用于所有功能设计文档中的详细设计章节。

### 标准列格式（11列，不可缩减）

```markdown
| 序号 | 字段名 | 控件类型 | 数值类型 | 长度 | 必输 Y/N | 新增 Y/N | 编辑 Y/N | 值列表说明 | 默认值 | 校验规则 | 其他说明 |
|------|--------|---------|---------|------|---------|---------|---------|-----------|-------|---------|---------|
| 1    | 字段A  | 文本    | 字符    | 200  | Y       | Y       | N       | -         | -     | 不能为空 | 创建后只读 |
| 2    | 字段B  | 下拉    | 字符    | 50   | Y       | Y       | Y       | 启用/禁用  | 启用  | 不能为空 | -       |
```

### 数值类型命名规范（中文）

| 数据库类型 | PRD写法 | 长度列 | 附加说明 |
|-----------|--------|-------|---------|
| VARCHAR / NVARCHAR | 字符 | 必填，如 200 | - |
| INT | 整数 | - | 注明范围，如（≥0） |
| DECIMAL | 小数 | 精度说明 | 如（保留2位小数） |
| DATE | 日期 | - | 注明格式，如（YYYY-MM-DD） |
| DATETIME | 日期时间 | - | 注明格式，如（YYYY-MM-DD HH:mm:ss） |
| BOOLEAN / TINYINT(1) | 布尔 | - | 值列表写"是/否" |
| TEXT | 长文本 | 可选 | 无长度限制时可写"-" |

### 新增 Y/N、编辑 Y/N 填写规则

| 页面类型 | 新增 Y/N | 编辑 Y/N | 备注 |
|---------|---------|---------|------|
| 只读详情页 | N | N | - |
| 新建表单 | Y | N | - |
| 编辑表单 | N | Y | - |
| 创建后只读字段 | Y | N | 其他说明注明"创建后只读" |
| 灰显但可见 | Y | N | 其他说明注明"创建后灰显" |

### 界面流转图规范

正式文档不使用 ASCII 字符流转图。应提供：

1. **HTML 流转图文件**（推荐）：保存为 `assets/flow-diagram.html`，在文档中注明路径和打开方式
2. **截图**：将 HTML 流转图截图嵌入文档
3. **引用表格**：补充流转来源/目标/触发方式/传参的表格说明

**HTML 流转图参考节点样式：**
- 绿色实色节点：入口页面（已存在）
- 蓝色实色节点：本次设计页面
- 白色虚线边框节点：外部跳转（不在需求范围）
- 蓝色背景操作节点：页面内操作（按钮行为）

### Word导出模板（reference.docx）

模板文件由 docx-js 生成，保存于 skills 根目录。**Typora 导出 Word 时配置 pandoc 模板路径指向此文件**，即可自动应用以下样式：

| 样式 | 字号 | 字重 | 字体 |
|------|------|------|------|
| H1 一级标题 | 18pt | 加粗 | 微软雅黑 |
| H2 二级标题 | 14pt | 加粗 | 微软雅黑 |
| H3 三级标题 | 12pt | 加粗 | 微软雅黑 |
| 正文内容 | 10pt | 常规 | 微软雅黑 |
| 表格内容 | 小五（9pt） | 常规 | 微软雅黑 |
| 页眉 | 小五（9pt） | 常规 | 微软雅黑 |

表格样式：全包黑色框线 + 表头浅蓝底色

---

## 模板11: PRD质控报告（自检门输出格式）

每次自检完成后，在PRD文档末尾追加以下格式的质控报告。

```markdown
## 质控报告（自动生成）

**自检时间：** YYYY-MM-DD HH:mm
**触发节点：** [完成章节名，如：第7章详细设计]
**评估总分：** X / 100（[等级：优秀/良好/需改进/不合格]）
**本次自检修复：** [N] 项（详见下方列表）
**待PM确认：** [N] 项（已写入"假设与待确认"表）

### 本次修复清单
- ① [修复项内容] → [修复方式，如：统一字段命名/补充默认值/完善验收标准]

### 本次待确认清单
| 待确认项 | 章节 | 缺失原因 | 影响范围 | 优先级 |
|---------|------|---------|---------|--------|
| [具体问题] | [影响章节] | 业务决策/外部依赖/权限/规则 | [影响说明] | 高/中/低 |

---

*（后续每次自检追加新报告，历史报告保留）*
```

**评分维度（5维度各20分，满分100）：**

| 维度 | 扣分触发 |
|------|---------|
| ① 完整性 | 必填章节缺失 → -10 |
| ② 无歧义 | 含模糊词（可能/大概/视情况）→ -5/处 |
| ③ 可追踪 | 功能在验收标准/详细设计中无对应 → -10/处 |
| ④ 一致性 | 同名不同义 / 同义不同名 → -5/处 |
| ⑤ 数据项完整性 | 数据项表格留空（非"-"） → -5/行 |

**等级与处置：**

| 总分 | 等级 | 动作 |
|------|------|------|
| 90-100 | 🟢 优秀 | 直接提交 |
| 70-89 | 🟡 良好 | AI自修复后再评，剩余标注PM |
| 50-69 | 🟠 需改进 | 修复缺口，剩余写入待确认表 |
| < 50 | 🔴 不合格 | 先说明缺口，请PM补充后再继续 |

